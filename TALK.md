# Run of show - `talk.ipynb`

*Generated by `build_talk.py`. Do not hand-edit; re-run the build.*

**59 cells** (22 code, 37 markdown) - **116.5s total live compute**

| Part | Budget | Live compute |
|---|---|---|
| 0 | 6 min | 0.9s |
| 1 | 8 min | 18.1s |
| 2 | 7 min | 2.9s |
| 3 | 7 min  *(+5 min optional)* | 3.1s (optional block only) |
| 4 | 8 min | 91.5s |
| 5 | 5 min | - |
| **Total** | **41 min** (46 with the optional block) | **116.5s** |

Against a 60-minute slot that leaves ~14 min for Q&A and drift.

> The optional block (Part 3's logit lens) costs **3.1s of compute** and about
> **5 minutes of speaking**. Nothing after it depends on it - verified: no name
> defined in that block is referenced later. Cut it at the podium for free.


## Title + setup

| # | do | beat |
|---|---|---|
| 0 | talk | How to Lobotomize an LLM |

## Part 0 - 6 min (clock 0:00 - 6:00)

| # | do | beat |
|---|---|---|
| 1 | talk | Part 0: A story you have probably heard |
| 2 | talk | Three corrections |
| 3 | talk | And now the experiment this whole talk is about |
| 4 | talk | Two things there, and the second is the one that matters |
| 5 | talk | Setup |
| 6 | **run** | `import warnings, numpy as np, torch, torch.nn as nn, matplotlib.py...` |

## Part 1 - 8 min (clock 6:00 - 14:00)

| # | do | beat |
|---|---|---|
| 7 | talk | Part 1: Compression — the one architecture this talk needs |
| 8 | **run** | `from torchvision import datasets, transforms` |
| 9 | **run** | `class AutoEncoder(nn.Module):` |
| 10 | talk | The same thing, drawn |
| 11 | **run** | `from matplotlib.patches import Circle, FancyBboxPatch` |
| 12 | talk | Look at what came out |
| 13 | **run** | `draw_funnel(0, ae=models[32])   # same picture, 32 numbers in the ...` |
| 14 | talk | How much of a digit survives a 2-number bottleneck? |
| 15 | **run** | `imgs = torch.stack([test[i][0] for i in range(8)]).to(DEV)` |
| 16 | talk | The bottleneck organises itself — without ever seeing a label |
| 17 | **run** | `zs, ys = [], []` |
| 18 | talk | Be honest about that plot. |

## Part 2 - 7 min (clock 14:00 - 21:00)

| # | do | beat |
|---|---|---|
| 19 | talk | Part 2: Latent spaces have directions |
| 20 | **run** | `Z_DIM = 32` |
| 21 | talk | First: what did it even generate? |
| 22 | **run** | `from sklearn.linear_model import LogisticRegression` |
| 23 | talk | The latent walk |
| 24 | **run** | `z0, z1 = a_latent_that_makes(0), a_latent_that_makes(1)` |
| 25 | talk | Look at what's in the middle |
| 26 | talk | Arithmetic on latent vectors |
| 27 | **run** | `def latent_direction(a, b):` |
| 28 | talk | One vector. Added to unrelated inputs. Consistent semantic change. |

## Part 3 - 7 min (clock 21:00 - 28:00)

| # | do | beat |
|---|---|---|
| 29 | talk | Part 3: What if the bottleneck were English? |
| 30 | talk | One substitution |
| 31 | talk | Does it work? |
| 32 | talk | The case study: Claude plans its rhymes |
| 33 | talk | The payoff: catching something the model never said |
| 34 | talk | Now let me undercut all of it |
| 35 | talk | &#9208; ⏸ OPTIONAL — cut this if you are behind the clock  *(~5 min)* |
| 36 | **run** | &#9208; `from transformers import AutoTokenizer, AutoModelForCausalLM` |
| 37 | talk | &#9208; Watch the last column |
| 38 | **run** | &#9208; `logit_lens("The Golden Gate Bridge is in San")` |
| 39 | talk | &#9208; And this is why the real paper trains a whole LLM for the job |

## Part 4 - 8 min (clock 28:00 - 36:00)

| # | do | beat |
|---|---|---|
| 40 | talk | Part 4: Lobotomy by scalpel — the refusal direction |
| 41 | **run** | `from transformers import AutoTokenizer, AutoModelForCausalLM` |
| 42 | talk | Every prompt below is **paired**: a request that trips the refusal behav... |
| 43 | **run** | `PAIRS = [` |
| 44 | talk | Measuring a refusal |
| 45 | **run** | `import re` |
| 46 | talk | Step 1 — screen the prompts on the intact model |
| 47 | **run** | `mask_h = refuses(HARMFUL, show=True)` |
| 48 | **run** | `mask_b = refuses(BENIGN, show=True)` |
| 49 | talk | Aside worth calling out: |
| 50 | **run** | `usable = [i for i in range(len(PAIRS)) if mask_h[i] and not mask_b...` |
| 51 | talk | Step 2 — difference in means. That's the whole method. |
| 52 | **run** | `@torch.no_grad()` |
| 53 | talk | Step 3 — project it out at every layer, and see if refusal survives |
| 54 | **run** | `layers = list(range(6, cm.config.num_hidden_layers, 2))` |
| 55 | **run** | `x = np.arange(len(layers)); w = 0.38` |
| 56 | talk | Read the layer-12 pair of bars. |

## Part 5 - 5 min (clock 36:00 - 41:00)

| # | do | beat |
|---|---|---|
| 57 | talk | Part 5: Back to the cold open |
| 58 | talk | Sources |

---

## Takeaways at a glance

| Part | TL;DR |
|---|---|
| 0 | You cannot measure a mind by interviewing it. Asking changes the answer, and the answer tracks the behaviour. |
| 1 | Force information through a narrow gap and it is obliged to become meaningful. What survives the squeeze is what mattered. |
| 2 | A trained network turns meaning into geometry. And once meaning is geometry, editing is arithmetic. |
| 3 | Make the bottleneck English and the compressed state becomes readable by construction — so you can catch a thought the model never said out loud. |
| 4 | Refusal is mediated by roughly one direction in activation space, which means alignment is far shallower than the effort that went into building it. |
| 5 | We can now read some of what a model is thinking without asking it. Badly, expensively, and about half the time. |

---

## Pre-flight

```
python build_talk.py                       # regenerate talk.ipynb
python -m ipykernel install --user --name lobotomy   # once, per venv
HF_HUB_OFFLINE=1 jupyter lab talk.ipynb    # offline: no download stalls on stage
```

- `models/mnist_dcgan_g.pt` must exist (`python scripts_train_gan.py`).
- MNIST must be in `data/` already - the download is not stage-safe.
- `gpt2` and `Qwen/Qwen2.5-0.5B-Instruct` must be in the HF cache.
- `scripts_prepare.py` / `data/acts_gpt2.npz` are **not** needed for this cut.
- Kernel must read **Python (lobotomize-llm)**, not the system Python.

